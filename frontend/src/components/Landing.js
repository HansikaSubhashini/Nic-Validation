import React from "react";

function Landing() {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        flexDirection: "column",
        fontFamily: "Arial, sans-serif",
      }}
    >
  <h1>Welcome to NIC Validator</h1>
      <p>Now you can validate NIC numbers easily.</p>
    </div>
  );
}

export default Landing;
